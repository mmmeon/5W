[package]
name = "fivew"
version = "0.1.3"
edition = "2024"
description = "A review-gated task queue over a markdown file, with stacked worktrees and a fast-forward ship"
license = "MIT"

[[bin]]
name = "5w"
path = "src/main.rs"

[dependencies]

[profile.release]
opt-level = 3
lto = true
strip = true
